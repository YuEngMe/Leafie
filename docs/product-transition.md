# 편지 기반 개편: 확정 사항과 구현 경계

## 문서와 코드의 상태

이 문서는 제품 목표이며 현재 배포 완료를 의미하지 않습니다. API 주소의 `/api/v1`은
유지하되 기존 계약을 바꾸는 기능별 PR에서 OpenAPI, migration과 앱 적용 순서를 함께
검토합니다. 기존 인증·푸시·사진 인식·진단 코드를 재사용합니다.

편지 DB·예약 함수·생성/공개 Worker·우편함 API 구현과 남은 다이어리/센서 연결 경계는
[편지 연동 가이드](letter-integration.md)에 기록합니다. 운영 자동 생성 활성화와 구분합니다.

## 확정된 변경

- AI 채팅을 제거하고 다이어리에 대응하는 LLM 편지를 제공합니다.
- 다이어리 한 건당 편지는 한 통이며 5~15분 랜덤 도착을 목표로 합니다.
- 다이어리를 수정해도 생성된 편지는 유지합니다.
- 식물 시작일은 생년월일과 D+의 기준입니다. 분갈이 날짜는 선택 입력입니다.
- 인식에서 아니에요를 누르면 다음 후보, 후보가 없으면 검색으로 이동합니다.
- 수동 컨디션 필드와 가지치기 일정을 제거합니다.
- 홈 해 아이콘은 교감 애니메이션, 배경은 시간대별 표시입니다.
- 센서 데이터·장치 연결·집계·판정은 다른 담당자의 문서와 구현 범위입니다.

## 구현 전에 확정할 세부 계약

다음은 문서 초안의 제안값입니다. 이슈에서 결론을 기록한 후 API와 migration에 반영합니다.

| 항목 | 현재 초안 / 확인할 내용 |
|---|---|
| 다이어리 | 확정: 5개 날씨 코드, 제목 최대 100자, 기존 행은 제목·날씨 `null` 보존 |
| 우편함 | 전체 식물 통합 목록과 식물 필터, 삭제·읽음 동작의 최종 화면 확인 |
| 정보 수정 | 확정: 애칭·장소·성격 변경, 성격 변경 이력 저장; 종·시작일 변경 제외 |
| 분갈이 | 선택 날짜 미입력 시 최초 일정 생략, 이미 예정된 분갈이에 대한 추가 동작 |
| 급수 | 기존 반복 일정과 센서 기반 물 요구가 함께 있을 때 중복 일정·알림의 책임 |
| 홈 | 확정: D+1, 06~18시 낮, 오늘 관리 유지, 센서 게이지는 별도 API |
| 프로필 | 집사 기간 계산 기준은 기존 구현과 앱 계약을 유지하고 임의 변경하지 않음 |

종 선택은 7개 대분류가 아닌 기존 23개 카탈로그 항목입니다. 모든 항목이 생물학적
단일 종인 것은 아니며 선인장은 `Cactaceae` 과 수준인 기존 예외를 유지합니다.

홈은 시작 당일을 D+1로 계산하고 사용자 현지 시각 06:00~17:59를 낮, 나머지를 밤으로
표시합니다. 오늘의 물주기·분갈이·비료 일정은 유지합니다. 성격별 실제 대사가 확정되기
전에는 `NORMAL` 키와 null 본문만 반환하며 센서 상태를 추측하지 않습니다. 센서 게이지는
별도 센서 API가 제공하고 홈은 이후 #52의 정규화 상태만 소비합니다.

## 편지 처리 원칙

- 다이어리 최초 저장 트랜잭션에서 편지 한 건과 즉시 실행할 생성 작업을 예약합니다.
  `scheduled_at`은 생성 시작이 아니라 공개 목표 시각이며 저장 시각 + 300~900초 중
  무작위로 한 번 정합니다. 재시도·다이어리 수정 때 다시 뽑지 않습니다.
- 생성은 저장 커밋 후 가능한 즉시 시작하고 완료 본문은 공개 시각까지 숨깁니다.
  생성 상태 `COMPLETED`만으로 목록·상세·미확인 개수·알림에 노출하면 안 됩니다.
- 예약 시각에 준비된 편지를 공개합니다. API 장애·Queue 지연으로 늦으면 생성 완료 후
  가능한 즉시 공개하며 15분 이내 도착을 무조건 보장하지 않습니다.
- 외부 API 일시 오류는 제한적으로 재시도합니다. 인증·요청 오류, 거절, 빈 본문과
  토큰 한도 등 불완전 응답은 영구 실패로 처리하고 잘린 본문을 편지로 저장하지 않습니다.
- 생성 입력은 Worker가 선점하여 읽은 시점의 스냅샷입니다. 그 이후 수정은 진행 중인
  외부 요청을 재시작하지 않으며 완성된 편지는 불변입니다.
- 다이어리의 사용자가 쓴 내용은 모델 명령이 아닌 자료로 취급합니다. 편지 생성에는
  업무 변경 Tool Calling이 필요하지 않습니다.
- 편지 unique는 soft delete 후에도 유지하여 삭제 후 재생성을 막습니다.
- 생성 완료 저장과 공개 작업 예약을 원자적으로 처리합니다. 공개 시각 기록, 도착 알림과
  푸시 Queue 등록도 한 트랜잭션으로 처리합니다. Worker 재전달이 중복 공개·알림을 만들지
  않아야 하며 삭제된 식물·계정·편지는 공개하지 않습니다.
- 장기 PROCESSING 복구, 재시도 한도와 삭제된 식물의 작업 취소를 구현합니다.
- 센서 요약의 실제 필드명은 확정하지 않습니다. 연동 전 테스트에서는 fixture를 사용하고
  운영 센서 값을 임의로 생성하지 않습니다.

## DB 전환 원칙

### 진단 분리 migration (#43)

`a7d921e4b603`은 `diagnoses.related_conversation_id`의 FK와 컬럼만 제거합니다.
진단 결과, 사진과 진단 완료 알림은 보존합니다. 기존 채팅 메시지와 전용 미디어
메타데이터는 `f3a7c9d42e10`에서 제거합니다.

기존 API·Worker는 제거할 컬럼을 참조하므로 배포 시 구버전 프로세스를 중지하고 진행
작업을 마친 뒤 migration을 적용하고 새 API·Worker를 시작합니다. 앱 요청에서도
`conversation_id`를 제거해야 합니다. 이번 PR은 공유 DB에 migration을 자동 적용하지 않습니다.

downgrade는 nullable 컬럼과 FK만 복구하며 이전 연결값을 복원하지 못합니다. 연결값 복구가
필요하면 적용 전 백업이 필요합니다.

### 공통 원칙

- 기존 migration을 지우지 않고 새 revision으로 변경합니다.
- ERD의 표는 변경 대상 중심의 개념 요약입니다. 생략된 기존 보안·감사·멱등성 필드를
  삭제 대상으로 해석하지 않습니다. 기존 필드명은 기능상 필요한 경우에만 변경합니다.
- 채팅 제거 migration 적용 전 구버전 프로세스를 중지하고 CHAT Queue 작업과 Storage
  객체를 정리합니다. migration은 채팅 데이터와 CHAT 미디어 메타데이터를 복원하지 않습니다.
- 전환 이전 다이어리는 편지를 소급 생성하지 않습니다. 해당 다이어리 수정도 생성
  트리거가 아니며 전환 후 최초 생성된 다이어리만 대상입니다.
- 전환 이전 다이어리는 제목과 날씨를 임의 생성하지 않고 `null`로 보존합니다. 조회는
  `null`을 반환하며 수정 시 제목·날씨를 필수로 받아 신규 계약으로 전환합니다.
- 다이어리 hard delete 시 `letters.diary_id ON DELETE CASCADE`로 연결 편지도 함께
  삭제합니다. 편지 Worker는 삭제된 다이어리를 공개하지 않습니다.
- 다이어리 v2 migration을 downgrade하면 제거했던 컨디션 점수는 복원할 수 없어
  중립값 `50`으로만 되돌립니다. 적용 전 복원이 필요하면 백업해야 합니다.
- 운영 데이터 삭제가 필요한 migration은 대상 행 수·백업·복구 한계를 PR에 기록하고
  실제 DB 적용 단계에서 확인합니다. 이번 문서 PR은 DB를 변경하지 않습니다.
- 관리 일정 정리 migration은 기존 `PRUNING`, `CUSTOM` 이벤트와 연결 알림, 홈 메모를
  삭제합니다. downgrade는 빈 홈 메모 테이블과 제거된 컬럼·제약만 복구하며 삭제한 행은
  복원하지 못하므로 실제 적용 전에 대상 행 수와 백업 여부를 확인합니다.
